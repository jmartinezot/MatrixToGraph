#ifndef __GeneralDefinitions__   
#define __GeneralDefinitions__

#include <vector>

typedef std::vector<std::vector<int> > AttributeMatrix;
typedef std::vector<std::vector<int> > AdjacencyMatrix;

#endif 
